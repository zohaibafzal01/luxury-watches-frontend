
import React from 'react';
import { ProfileSettings as ProfileSettingsComponent } from '@/components/common/ProfileSettings';

const ProfileSettings = () => {
  return <ProfileSettingsComponent />;
};

export default ProfileSettings;
